$kind: http-request
description: GDPR Art. 17 anonymization preserving the 7-year tax ledger.
url: "{{base_url}}/customers/{{customer_id}}/erasure-requests"
method: POST
headers:
  Authorization: Bearer {{token}}
  Accept: application/json
order: 2000
