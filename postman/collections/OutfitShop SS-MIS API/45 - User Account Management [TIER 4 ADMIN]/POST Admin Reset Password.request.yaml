$kind: http-request
description: Ops reset without mailer; revokes all target sessions; audited on the admin channel.
url: "{{base_url}}/auth/admin-reset-password"
method: POST
headers:
  Authorization: Bearer {{token}}
  Accept: application/json
body:
  type: json
  content: |-
    {
      "email": "staff@api.kesararamwithdigital.tech",
      "new_password": "Rotated123!"
    }
order: 2000
