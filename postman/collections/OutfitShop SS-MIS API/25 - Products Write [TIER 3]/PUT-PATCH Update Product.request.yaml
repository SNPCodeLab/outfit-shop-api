$kind: http-request
name: PUT|PATCH Update Product
url: "{{base_url}}/products/{{product_id}}"
method: PATCH
headers:
  Authorization: Bearer {{token}}
  Accept: application/json
body:
  type: json
  content: |-
    {
      "description": "Updated description"
    }
order: 2000
