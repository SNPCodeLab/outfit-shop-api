$kind: http-request
description: Upload-and-attach convenience (multipart).
url: "{{base_url}}/products/{{product_id}}/image"
method: POST
headers:
  Authorization: Bearer {{token}}
  Accept: application/json
order: 6000
